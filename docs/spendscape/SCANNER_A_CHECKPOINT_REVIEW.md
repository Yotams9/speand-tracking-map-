# Scanner A checkpoint review — 2026-09-07

Authorization: `APPROVE SPENDSCAPE SCANNER A CHECKPOINT REVIEW — PHYSICAL IPHONE QA PENDING HTTPS PREVIEW`.

Verdict: **ready for a separately approved local checkpoint commit**. No
production implementation or tracked test was changed during this review.
Physical iPhone Safari QA remains pending an explicitly authorized HTTPS Preview
deployment. This is not physical-device, Safari, deployment or production approval.

## Findings by severity

| Severity | Open findings |
| --- | --- |
| Blocker | None |
| High | None |
| Medium | None |
| Low | None |
| Informational | Physical iPhone Safari testing is explicitly deferred by the user. Android physical testing is also not established by this review. |
| Commit exclusion | Pre-existing generated `next-env.d.ts` drift and all generated artifacts must remain outside the checkpoint diff. |

## Requirements and evidence

| Requirement | Review result |
| --- | --- |
| Explicit user action | Only the camera button invokes the session start function. Mounting Capture and navigating its demo/manual paths never call getUserMedia. Verified in source, unit and browser tests. |
| Video only | Both the preferred rear-camera request and the relaxed-constraints retry specify `audio: false`. No microphone request or enumeration code was added. |
| No frame transfer or retention | The only media sink is `video.srcObject`, followed by `video.play()`. No frame extraction, encoder, recorder, image serialization, upload, provider, log or storage path exists in the new runtime modules. |
| All tracks stop | The central release path calls `getTracks().forEach(stop)`, removes listeners, pauses video and clears srcObject. Browser review probes used two video tracks to verify complete release on live Close, Escape, Back, Sources, Manual, Demo and video failure. Existing tests cover Stop, background/pagehide, track interruption and unmount. |
| Cancellation and late grants | Generation/disposal checks stop late-acquired tracks without attaching them. Existing tests cover eight pending cancellation/navigation paths. An additional close/reopen probe verified a stale grant cannot replace or stop the new preview, and all old tracks end. |
| Permission/error/retry states | Denial, blocked permission, no camera, busy camera, insecure context, unsupported browser, interruption and playback failure are handled. Extra browser probes verified explicit retry after denial, missing and busy camera errors; no automatic permission retry occurs. |
| Existing product behavior | Fresh regression coverage passed for Capture demos/manual input, Globe, Inbox, Ask preview/Apply/Undo and focus, stationary Replay/explicit Show place, Purchases, filters/search, Timeline and Analytics. |
| Canonical baseline | 42 purchases, 12 places and 12 canonical pins before session actions; ILS 6777.38 total; two online purchases and one unresolved purchase remain excluded from pins. One MapLibre construction and instance is preserved. |
| Scope audit | Complete tracked and untracked Scanner A diff reviewed. Package manifest/lockfile, data graph, repository adapter, app routes and backend configuration are unchanged. No dependency, provider, database, authentication, credential, account or real-data integration was added. |

The existing globe's fields named `camera` and its sessionStorage entries refer
to map viewpoint coordinates, not video frames or MediaStreams. The new stream
is not part of that state, the repository, or the app's QA snapshot.

Test fixtures create synthetic canvas streams and may capture those artificial
pixels in QA screenshots/traces. These are test-only inputs; the app adds no
image capture or persistence path. No physical camera was accessed in review.
Existing MapLibre tile/style traffic remains unchanged.

## Verification actually run

- `npm run typecheck`: passed.
- `npm test`: **126/126 passed**, including 22 camera lifecycle tests.
- `npm run build`: passed; application remains statically rendered.
- Production Chrome regression run: **55/55 passed**, no failures, skips or
  flaky results. Included Scanner A (20), Capture (5), boundary (4), Inbox (4),
  Ask (12), Analytics (2), Purchases/history (2), stationary Replay (6).
- Supplemental review probes: **11/11 passed**, no failures, skips or flaky
  results. Covered multiple tracks, all live exit paths, explicit retry and a
  late permission grant after closing and reopening Capture.
- `git diff --check`: passed. The staging area remained empty.

Both browser runs used the same production build on 127.0.0.1:3000. Local review
reports and supplemental probes are ignored under
`artifacts/spendscape-scanner-a-review/`, including `regression-report.json` and
`extra-report.json`. Camera screenshots were regenerated by the focused suite;
Hebrew/RTL and landscape previews were visually re-inspected. Viewport coverage
includes 360x640, 390x844, 430x932, 1440x900 and 844x390 landscape.

## Exact future checkpoint commit scope

Only after receiving the separate commit token below, use explicit paths and
verify the staged diff contains only these reviewed files:

```text
docs/spendscape/README.md
docs/spendscape/SCANNER_A_CHECKPOINT.md
docs/spendscape/SCANNER_A_CHECKPOINT_REVIEW.md
playwright.scanner-a.config.ts
qa/spendscape-capture.spec.ts
qa/spendscape-scanner-a.spec.ts
src/features/capture/CaptureCamera.tsx
src/features/capture/CaptureExperience.module.css
src/features/capture/CaptureExperience.tsx
src/features/capture/camera-session.test.ts
src/features/capture/camera-session.ts
src/features/capture/useCaptureCamera.ts
```

Exclude **next-env.d.ts**, `.next/`, `artifacts/`, `playwright-report/`,
`test-results/`, coverage, caches and generated recordings/screenshots. No
generated artifact is tracked; the existing ignore rules cover those output
directories. `next-env.d.ts` is tracked, so ignore rules alone cannot exclude
its diff: never use broad staging and explicitly verify its staged diff is empty.
If any reviewed implementation changes before commit, review that change first.

## Git and cleanup

- Branch: `feature/spendscape-rebuild`.
- HEAD and upstream: `c17595c3e1d00138c278a389367731da8f3bf644`.
- Main and origin/main: `eee0d26b55e5061f87ac664938df0c195800b74f`.
- The pre-review `next-env.d.ts` contents were preserved and restored exactly;
  its pre-existing local drift is intentionally not included in the allowlist.
- The review's production QA server was stopped. No commit, push, PR,
  deployment, Vercel connection, account or dependency installation occurred.
- This review adds/updates only review documentation and ignored QA evidence.
  Runtime and tracked test hashes are unchanged from the start of review.

## Separate approval required

```text
APPROVE SPENDSCAPE SCANNER A CHECKPOINT COMMIT
```

This token would authorize only the reviewed local commit with the exclusions
above and physical iPhone QA still pending HTTPS Preview. It does not authorize
push, deployment, Vercel, accounts, Scanner B, providers or any later phase.
